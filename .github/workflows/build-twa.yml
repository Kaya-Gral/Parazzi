name: Build TWA

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Bubblewrap
        run: npm install -g @bubblewrap/cli

      - name: Init TWA project (Python pty)
        timeout-minutes: 5
        run: |
          mkdir -p twa
          python3 << 'PYEOF'
          import pty, os, subprocess, select, time, sys

          master, slave = pty.openpty()
          env = os.environ.copy()
          env['CI'] = 'true'
          
          p = subprocess.Popen(
            ['npx', '@bubblewrap/cli', 'init',
             '--manifest', 'https://parazzi.vercel.app/manifest.json',
             '--directory', './twa'],
            stdin=slave, stdout=slave, stderr=slave,
            env=env
          )
          os.close(slave)

          answer_count = 0
          while p.poll() is None and answer_count < 100:
            ready, _, _ = select.select([master], [], [], 0.5)
            if ready:
              try:
                data = os.read(master, 4096).decode(errors='ignore')
                sys.stdout.write(data)
                sys.stdout.flush()
                if any(c in data for c in ['?', ':', '>']) and len(data.strip()) > 0:
                  time.sleep(0.5)
                  os.write(master, b'\n')
                  answer_count += 1
              except OSError:
                break

          os.close(master)
          p.wait(timeout=300)
          sys.exit(p.returncode)
          PYEOF

      - name: Generate signing keystore
        run: |
          cd twa
          keytool -genkey -v -keystore android.keystore -alias android -keyalg RSA -keysize 2048 -validity 10000 -storepass android -keypass android -dname "CN=Parazzi"

      - name: Build APK and AAB (Python pty)
        timeout-minutes: 15
        run: |
          cd twa
          python3 << 'PYEOF'
          import pty, os, subprocess, select, time, sys

          master, slave = pty.openpty()
          env = os.environ.copy()
          env['CI'] = 'true'
          
          p = subprocess.Popen(
            ['npx', '@bubblewrap/cli', 'build'],
            stdin=slave, stdout=slave, stderr=slave,
            env=env
          )
          os.close(slave)

          answer_count = 0
          while p.poll() is None and answer_count < 100:
            ready, _, _ = select.select([master], [], [], 0.5)
            if ready:
              try:
                data = os.read(master, 4096).decode(errors='ignore')
                sys.stdout.write(data)
                sys.stdout.flush()
                if any(c in data for c in ['?', ':', '>']) and len(data.strip()) > 0:
                  time.sleep(0.5)
                  os.write(master, b'\n')
                  answer_count += 1
              except OSError:
                break

          os.close(master)
          p.wait(timeout=900)
          sys.exit(p.returncode)
          PYEOF

      - name: Find built APK
        run: find twa -name "*.apk" -o -name "*.aab"

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: parazzi-apk
          path: twa/**/*.apk

      - name: Upload AAB
        uses: actions/upload-artifact@v4
        with:
          name: parazzi-aab
          path: twa/**/*.aab
